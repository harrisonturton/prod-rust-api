import { Router } from "../router";

describe("Router", () => {
    it("doesn't have trailing `?` when query parameters are undefined", () => {
        // TODO(harry): implement
    });

    it("serializes query parameters to expected string", () => {
        // TODO(harry): implement
    });
});
