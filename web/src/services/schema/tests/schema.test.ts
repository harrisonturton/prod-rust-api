import { checkSchema } from "../schema";

describe("checkSchema", () => {
    it("rejects values that don't match schema", () => {
        // TODO(harry): implement
    });

    it("accepts values that match schema", () => {
        // TODO(harry): implement
    });
});
