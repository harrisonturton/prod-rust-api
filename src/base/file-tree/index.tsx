import styles from "./styles.module.scss";

const FileTree: React.VFC = () => (
    <div className={styles.container}>
        <span className={styles.label}>File tree</span>
    </div>
);

export default FileTree;